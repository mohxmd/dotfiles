#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
profile="${1:-auto}"

echo "[dotfiles] Applying profile: $profile"
"$ROOT_DIR/setup.sh" --profile "$profile"
